# Breedables Data

Data-driven definitions used by the engine.

- `genes/` — gene definitions and inheritance rules
- `traits/` — phenotype traits
- `colors/` — color definitions and mappings
- `patterns/` — coat/skin pattern definitions
- `rarity/` — rarity tiers and probability tables
- `mutations/` — rare mutation definitions
